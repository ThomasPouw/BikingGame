using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AllowedElements : MonoBehaviour
{
    [SerializeField] public List<GameObject> AllowedQuestions;
    [SerializeField] public List<GameObject> AllowedWayPoints;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
